# Server
NODE_ENV=development
PORT=3001
API_PREFIX=api/v1

# Database (PostgreSQL, хостинг в РФ)
# Пример для VPS по docs/DEPLOY.md §2.3: пользователь winwin, пароль admin-winwin
DATABASE_URL="postgresql://winwin:admin-winwin@localhost:5432/winwin"

# Redis (self-hosted в РФ)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Meilisearch (self-hosted в РФ)
MEILISEARCH_HOST=http://localhost:7700
MEILISEARCH_API_KEY=

# JWT
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# Первичный администратор — после миграций: npx prisma db seed
# Email нормализуется в нижний регистр; у пароля обрезаются пробелы по краям.
ADMIN_SEED_EMAIL=admin@winwin.local
ADMIN_SEED_PASSWORD=change-me-admin

# Сессии / cookie (опционально)
SESSION_SECRET=session-secret-change-in-production
